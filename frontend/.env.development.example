### Example Vite environment file for development
# Copy this to `.env.development` and fill real values locally.

# Base API URL used by `frontend/src/api.js` (must start with http:// or https://)
VITE_API_URL=http://localhost:8000

# Example additional keys (prefix with VITE_ to expose to client)
# VITE_SUPER_KEY=your_value_here
